<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DBP_Cv_Form_ultimo.CVFormPage">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <!-- T�tulo -->
            <Label Text="Formulario para CV" FontSize="Large" HorizontalOptions="Center" />

            <!-- Campos del formulario -->
            <Entry x:Name="NameEntry" Placeholder="Nombre completo" />
            <Entry x:Name="AgeEntry" Placeholder="Edad" Keyboard="Numeric" />
            <Entry x:Name="OccupationEntry" Placeholder="Ocupaci�n" />
            <Entry x:Name="AddressEntry" Placeholder="Direcci�n" />

            <!-- Bot�n para generar el CV -->
            <Button Text="Generar CV" BackgroundColor="Blue" TextColor="White" Clicked="OnGenerateCVButtonClicked" />

            <!-- �rea para mostrar el CV -->
            <Label x:Name="CVLabel" FontSize="Medium" TextColor="Black" HorizontalOptions="CenterAndExpand" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
